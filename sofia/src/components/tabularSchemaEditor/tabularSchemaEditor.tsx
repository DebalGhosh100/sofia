import React, {
  useEffect,
  useState,
  Dispatch,
  SetStateAction,
  forwardRef,
  useImperativeHandle,
} from "react";

import styles from "./tabularSchemaEditor.module.scss";

import { AnyOf, Property, Schema } from "@/types/schema";

import { DataType } from "@/types/dataType";

export type TabularSchemaEditorPropsType = {};

const TabularSchemaEditor = (props: TabularSchemaEditorPropsType) => {
  return <div></div>;
};

export default TabularSchemaEditor;
